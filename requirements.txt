# Core dependencies
Flask==2.3.2
gunicorn==21.2.0
python-dotenv==1.0.0

# Optional utilities (uncomment if needed)
requests==2.31.0
# Ensure this line is NOT constrained to an old version (e.g., no ==0.3.2)
google-generativeai
